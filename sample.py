def call_me_sample(query : str):
    return 123




# results = hybrid_search(query, top_k=10)

# relevant_chunks = [
#     r for r in results 
#     if r["_score"] > THRESHOLD
# ]

# if len(relevant_chunks) >= 6:
#     # Use existing vector memory
# else:
#     # Call Serper and fetch new papers